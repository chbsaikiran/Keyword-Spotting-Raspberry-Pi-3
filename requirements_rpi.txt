# Raspberry Pi 3 — armv7l, Python 3.9, 32-bit OS
# Install: pip install -r requirements_rpi.txt

tflite-runtime          # 2.13.0 armv7l wheel confirmed working
numpy                   # resolved from piwheels; libopenblas0 must be installed
scipy                   # fallback STFT when librosa unavailable
soundfile               # reading wav/flac files
sounddevice             # real-time microphone capture (optional)
librosa                 # mel spectrogram (optional, falls back to scipy)
