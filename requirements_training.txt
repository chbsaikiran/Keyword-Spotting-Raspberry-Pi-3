# Training machine (Linux/Windows/macOS, GPU recommended)

# Core training
torch>=2.3.0
torchaudio>=2.3.0
numpy>=1.26.0

# TFLite export + INT8 quantization
litert-torch            # PyTorch → TFLite via PT2E (replaces onnx-tf pipeline)
tensorflow>=2.13.0      # TFLite converter + evaluation
tqdm
